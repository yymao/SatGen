[project]
name = "SatGen"
version = "2023.04.16"
dependencies = [
    "numpy",
    "scipy",
    "fast-histogram",
    "cosmolopy",
    "lmfit",
]

[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"
